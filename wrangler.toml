# Fichier de configuration pour Cloudflare Pages Functions
name = "bolt-diy-worker"
pages_build_output_dir = "build/client" # Chemin vers les assets statiques (Remix)

# Configuration essentielle pour activer les modules Node.js
compatibility_date = "2024-05-15" 
compatibility_flags = ["nodejs_compat"]

# Ajoutez ici vos bindings (KV, D1, etc.) si nécessaire.
# [[kv_namespaces]]
# binding = "YOUR_KV_BINDING_NAME"
# id = "YOUR_KV_ID"
